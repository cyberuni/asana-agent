{"seq": 1, "kind": "leash", "cr": "github-162", "leash": "auto-all", "by": "derived", "approach": ["hybrid node shape: one behavioral skills/ node owning the cross-skill catalog contract, plus one reference node per shipped skill", "placement map gains a skills route and a routing table for the packaging and ACED-boundary tie-breaks", "the catalog rules already enforced in the test file are extracted to a checked contract so the suite's negative edges are verifiable"], "why": "additive spec corpus plus a test-only extraction; no narrowing of a frozen scenario, no published-surface change, no new hard floor"}
{"seq": 2, "kind": "gate", "cr": "github-162", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "check-suite green: every scenario binds 1:1 to a scenario-map row, every map row names a real scenario, no duplicate edge and path pair. Every guard edge carries a positive companion on the same path. All nine shipped skills placed as reference nodes; behavioral evaluation of a skill routed out of this corpus by an explicit routing-table row rather than left implicit.", "frozen": ["skills/skills.feature"]}
